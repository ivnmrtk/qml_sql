# Сервис работы с БД, содержащей информацию о книгах
Файл с данными - books.db
```sql
create table books
(
    id        INT,
    title     VARCHAR(255),
    author    VARCHAR(255),
    publisher VARCHAR(255)
);
```
